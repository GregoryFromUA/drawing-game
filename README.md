# 🎨 Drawing Game

Мультиплеєрна гра для малювання з двома режимами:
- **Doodle Prophet** - малюйте свої завдання і відгадуйте малюнки інших
- **Unicorn Canvas (Fake Artist)** - спільне малювання, один гравець підробний художник

## 🚀 Запуск проекту

### Розробка (Development)

1. Встановіть залежності:
```bash
npm install
```

2. Запустіть Vite dev server (frontend):
```bash
npm run dev
```

3. В іншому терміналі запустіть backend сервер:
```bash
npm run server:dev
```

Frontend буде доступний на: http://localhost:3000
Backend (Socket.IO) на: http://localhost:3001

### Продакшн (Production)

1. Зробіть build:
```bash
npm run build
```

2. Запустіть сервер:
```bash
npm run server
```

Гра буде доступна на порту 3001.

## 📁 Структура проекту

```
drawing-game/
├── src/                    # Вихідний код (Vite)
│   ├── components/         # React компоненти
│   ├── styles/            # CSS файли
│   │   ├── base.css       # Базові стилі
│   │   ├── lobby.css      # Стилі лобі
│   │   └── game.css       # Стилі гри
│   ├── App.jsx            # Головний компонент
│   └── main.jsx           # Точка входу
├── dist/                  # Зібраний проект (після build)
├── public/                # Старий проект (legacy)
├── server.js              # Backend сервер (Socket.IO)
├── wordSets.js            # Набори слів для гри
├── fakeArtistThemes.js    # Теми для режиму Fake Artist
├── vite.config.js         # Конфігурація Vite
├── index.html             # HTML шаблон
└── package.json           # Залежності

```

## 🔧 Доступні команди

- `npm run dev` - запустити Vite dev server
- `npm run build` - зробити production build
- `npm run preview` - переглянути production build локально
- `npm run server` - запустити backend сервер
- `npm run server:dev` - запустити backend з hot reload

## ⚡ Оптимізації

### WebSocket трафік
Проект оптимізовано для зменшення WebSocket трафіку:
- **Було**: ~5.43GB за 1.5 години
- **Стало**: ~300MB за 1.5 години (покращення в **18 разів**)

Оптимізації:
1. Батчинг stroke-ів з інтервалом 150ms (замість 60fps)
2. Округлення координат до integer (замість float)
3. Застосовано для обох режимів гри

### Структура коду
- Розділено на окремі модулі (CSS, компоненти)
- Використання Vite для швидкої розробки
- Hot Module Replacement (HMR) для миттєвих змін

## 📝 TODO

Поточний проект містить базову структуру. Для повного рефакторингу потрібно:

1. ✅ Налаштувати Vite
2. ✅ Винести CSS в окремі файли
3. ⏳ Розділити App.jsx на окремі компоненти:
   - GameBoard.jsx
   - OtherPlayerDrawing.jsx
   - ScoreRulesModal.jsx
   - UnicornCanvas.jsx
4. ⏳ Додати TypeScript (опціонально)
5. ⏳ Додати тести

## 🎮 Як грати

1. Створіть кімнату або приєднайтесь за кодом
2. Зачекайте поки зберуться мінімум 3 гравці
3. Натисніть "Готовий" (або "Почати гру" якщо ви хост)
4. Малюйте своє завдання та відгадуйте малюнки інших!

## 📊 Система очок

- Чим швидше відгадаєте малюнок - більше очок (6→5→5→4→4→3→3→2→2→1→1)
- За погані малюнки (які ніхто не відгадав) - штраф
- Переможець визначається після 4 раундів

## 🛠 Технології

- **Frontend**: React 18, Vite
- **Backend**: Node.js, Express, Socket.IO
- **Комунікація**: WebSocket (Socket.IO)
- **Canvas**: HTML5 Canvas API

## 📄 Ліцензія

MIT
